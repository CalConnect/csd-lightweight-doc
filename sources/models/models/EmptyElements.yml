# Converstion from EmptyElements.wsd
# Created by: Vasil Buraliev (vasil.buraliev@vbu.mk)
# Create date: 25/09/2019

# * `LinkBreakElement` is `<br>`
# * `HorizontalRuleElement` is `<hr>`
# * `PageBreakElement` only works in page layout\n(e.g., PDF, Word), not in flow layouts (e.g., HTML)
title: Empty Elements
classes:
  BasicElement:
    definition: |
    
  PageBreakElement:
    definition: |

    relations:
      - target: BasicElement
        relationship:
          target:
            type: inheritance
        direction: up
  LineBreakElement:
    definition: |

    relations:
      - target: BasicElement
        relationship:
          target:
            type: inheritance
        direction: up
  HorizontalRuleElement:
    definition: |

    relations:
      - target: BasicElement
        relationship:
          target:
            type: inheritance
        direction: up

# This enumerator is implemented in a separate file but it is used in DataTypes.yml
# Created by: Vasil Buraliev (vasil.buraliev@vbu.mk)
# Create date: 25/09/2019

# The coresponsind MIME types,\ndefaults to "text/plain"\n(docbook / asciidoc / isdoc\nare not registered types yet)
name: StringFormat
modelType: enum
type: enumeration
values:
  plain:
    definition:
  html:
    definition:
  docbook:
    definition: 
  tei:
    definition:
  asciidoc:
    definition:
  markdown:
    definition:
  standarddoc:
    definition:
  isodoc:
    definition:
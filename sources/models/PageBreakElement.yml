# * `PageBreakElement` only works in page layout\n(e.g., PDF, Word), not in flow layouts (e.g., HTML)
name: PageBreakElement
modelType: class
definition: |

relations:
  - target: BasicElement
    relationship:
      target:
        type: inheritance
    direction: up